import MalWrapper from './animeProcessing/components/MalWrapper';
import { FiLogger, colors, logLvl } from './utils/FiLogger';

const log: FiLogger = FiLogger.getInstance();

let test = new MalWrapper();
